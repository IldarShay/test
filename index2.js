let arrNumbers = [7, 8, 9, 10, 0, 1, 2, 3, 4, 5, 6];

let search = function (arrNumbers, target) {
  let left = 0;
  let right = arrNumbers.length - 1;

  while (left <= right) {
    let mid = Math.floor((left + right) / 2);
  }
  if (arrNumbers[left] < arrNumbers[mid]) {
    return;
  }

  if (arrNumbers[mid] === max) return max;
};
